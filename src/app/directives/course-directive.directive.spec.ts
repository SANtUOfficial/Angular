import { CourseDirectiveDirective } from './course-directive.directive';

xdescribe('CourseDirectiveDirective', () => {
  it('should create an instance', () => {
    //const directive = new CourseDirectiveDirective();
    //expect(directive).toBeTruthy();
  });
});
